# Cudos-MultiSend dApp

## running in Docker
 - `docker build -t cudos-multisend . `
 - `docker run --rm -it -p 3033:80 cudos-multisend`

## running Locally
 - `yarn install`
 - `yarn build`
 - `yarn dev`
